<% provide(:title, t(".title", course: @course.id)) %>
<div class="container-fluid overflow-auto py-3">
  <div class="card">
    <div class="card-header">
      <%= content_tag :h3, t(".course", course: @course.id) %>
    </div>
    <div class="card-body">
      <%= render Admin::CourseFormComponent.new(course: @course, variant: :edit) %>
    </div>
  </div>
<div>
